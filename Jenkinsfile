pipeline {
    agent { node { label 'hieudt37-jenkins-agent-permanent' } }
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello Word"'
            }
        }
    }
}

